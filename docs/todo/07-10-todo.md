# Todo 2026-07-10

> 建立日期:2026-07-04
> 預計執行日期:2026-07-10

## 已完成（07-04 收尾）
- [x] cost-6 建築基本分修正：`city_hall` / `guild_hall` / `palace` / `triumphal_arch` / `residence` `vp` 改為 0，分數改由終局計分規則計算（`data/cards.buildings.json`、`tests/unit/data/cards.test.ts`）
- [x] Branch 3 `feat/api-foundation`：3-1 環境與 Auth
  - `.env.example`（`DISCORD_CLIENT_ID`/`SECRET`/`REDIRECT_URI`、`SESSION_SECRET`）
  - `GET /api/auth/discord/login`、`GET /api/auth/discord/callback`、`POST /api/auth/logout`、`GET /api/users/info`
  - `server/utils/authSession.ts`、`server/utils/discord.ts`（session 用 h3 `useSession`，OAuth token 不落地，僅存最小 profile）
  - `useAuth.ts` composable（`isLoggedIn`/`player`/`login()`/`logout()`，以 `useState` 避免 SSR 跨請求污染）
  - OAuth token 交換 mock 測試 → `tests/unit/server/discord-callback.nuxt.test.ts`
  - `npm run lint` + `npm run build` + `npm run test` 全通過

---

## 高優先（本週必須完成）

- [x] **Branch 3 `feat/api-foundation`：3-2 房間 / 大廳 API**
  - `GET /api/rooms`、`POST /api/rooms`、`GET /api/rooms/:roomId`
  - `POST /api/rooms/:roomId/join`、`leave`、`PUT ready`、`POST kick/:playerId`、`POST start`
  - Nitro in-memory Map 暫存房間資料（無後端 DB，重啟清空，V0.1 可接受）
  - 建立 `useRoom.ts` composable（`rooms`、`currentRoom`、`createRoom()`、`joinRoom()`、`leaveRoom()`）
  - 已補 `server/utils/roomStore.ts` 與房間規則單元測試（滿房、ready/start、host-only、host leave close）
  - 手動驗證清單：
    - [x] 啟動 dev server：`npm run dev`，開啟 `http://localhost:3000`
    - [x] 未登入時呼叫 `GET /api/rooms`，應回傳 `401 AUTH_ERROR`
    - [ ] 完成 Discord 登入後，開啟首頁或 `/lobby` 可正常載入，不應出現 client error
    - [ ] 登入後呼叫 `GET /api/users/info`，應回傳自己的 `discordId`、`username`、`avatar`
    - [ ] 呼叫 `POST /api/rooms`，body 使用 `{ "visibility": "public" }`，應建立 `waiting` 房間，且自己是 `isHost: true`、`isReady: true`
    - [ ] 呼叫 `GET /api/rooms`，應看到剛建立的公開房間
    - [ ] 呼叫 `GET /api/rooms/:roomId`，應取得同一間房間完整資料
    - [ ] 使用第二個登入 session 或測試工具加入房間：`POST /api/rooms/:roomId/join`，新玩家應出現在 `players`
    - [ ] 非房主呼叫 `POST /api/rooms/:roomId/start`，應回傳 `403 HOST_ONLY`
    - [ ] 房主在未滿 4 人時呼叫 `POST /api/rooms/:roomId/start`，應回傳 `409 ROOM_NOT_FULL`
    - [ ] 玩家呼叫 `PUT /api/rooms/:roomId/ready`，body 使用 `{ "isReady": true }`，該玩家 `isReady` 應變為 `true`
    - [ ] 滿 4 人且所有玩家 ready 後，房主呼叫 `POST /api/rooms/:roomId/start`，房間 `status` 應變為 `playing`
    - [ ] 房主呼叫 `POST /api/rooms/:roomId/kick/:playerId` 踢非房主玩家，該玩家應從 `players` 移除
    - [ ] 非房主呼叫 `POST /api/rooms/:roomId/leave`，該玩家應從房間移除
    - [ ] 房主呼叫 `POST /api/rooms/:roomId/leave`，房間應關閉；再次查詢該房間應回傳 `404 ROOM_NOT_FOUND`
    - [ ] 重啟 dev server 後呼叫 `GET /api/rooms`，先前房間應消失（V0.1 in-memory Map 預期行為）
- [ ] **Branch 3：3-2-b 房間 Socket.io 即時事件**
  - 導入 `socket.io` / `socket.io-client`（已徵得同意）
  - Namespace `/game`：`server:room_joined`、`server:room_updated`、`server:player_kicked`、`server:room_closed`、聊天室（`client:send_message` / `server:chat_message`）
  - `app/plugins/socket.client.ts` 初始化 client
  - 整合進 `useRoom.ts`
- [ ] **Branch 3：3-3 遊戲狀態 API**
  - `GET /api/games/:gameId/state`、`GET /api/games/:gameId/result`、`GET /api/games/:gameId/log`
- [ ] **Branch 3：3-4 驗收**：`npm run lint` + `npm run build` + `npm run test` 全通過，使用者驗收 UI

## 中優先（本週目標）

- [ ] **補擴充新增生產建築 4 張**：確認確切貨物種類後
  - 加入 `data/cards.buildings.json`
  - 更新 `data/rules.config.json` 的 `pendingExpansionProduction`（4 → 0）與 `expectedCardCount`（expansion 30→34、total 140→144）
  - 更新 `tests/unit/data/cards.test.ts` 預期張數

## 低優先（有餘力再做）

- [ ] 消除 GitHub Actions Node 20 deprecation 警告：升級 `actions/checkout`、`actions/setup-node` 至支援 Node 24 的版本（`.github/workflows/ci.yml`）

## 待釐清（需討論後才能動）

- [ ] 擴充新增生產建築 4 張的確切種類（哪些貨物 / 是否為新類型）

## 備註
- Branch 3 範圍較大，已與使用者確認採分階段驗收：3-1 → 3-2 → 3-2-b → 3-3，逐段跑完六步驟流程再繼續下一段
- CI 已固定 npm 11 + Node 22（與本機一致），`npm ci` 不再因 lockfile 不同步而失敗
- `npm run test` / `test:e2e` 已加 `--passWithNoTests` / `--pass-with-no-tests`，待實際測試補上後仍相容

# 06/26 TODO
> 建立日期:2026-06-12
> 預計執行日期:2026-06-26

- [x] 規劃專案結構
- [] 設計假資料表
- [] 開始開發 api

---

## 分支計畫

### Branch 1：`chore/project-structure`
**目標**：把 Nuxt 4 目錄骨架從 bare `app.vue` 搭建完整

- [x] 建立頁面骨架（空白 `<template>` + `<script setup>`）→ `app/pages/index.vue`、`login.vue`、`lobby.vue`、`room/[id].vue`、`game/[id].vue`
- [x] 建立 layout → `app/layouts/default.vue`
- [x] 建立 composables 骨架（interface / 空 export）→ `useAuth.ts`、`useRoom.ts`、`useGameEngine.ts`、`usePersistence.ts`、`useOnboarding.ts`
- [x] 建立 TypeScript 型別定義 → `types/game.ts`、`types/room.ts`、`types/player.ts`、`types/events.ts`
- [x] 建立 `server/api/auth/discord/` 目錄骨架
- [x] 建立 `data/` 目錄
- [x] `npm run lint` + `npm run build` 通過

**Commit**：`chore(scaffold): set up nuxt4 directory structure and core type definitions`

---

### Branch 2：`chore/game-data-json`
**目標**：完整建置遊戲靜態 JSON 資料 + 驗證測試

- [x] 讀完 `docs/rules/san-juan-cards.md`，定義 JSON 欄位規格（`id`、`name`、`nameZh`、`category`、`cost`、`vp`、`count`、`goodType`/`sellValue`、`tags`、`effect{phase,text}`）
- [x] 建立生產建築資料（5 種，共 42 張）→ `data/cards.buildings.json` 生產建築區塊
- [x] 建立城市建築資料（紫色特殊建築，共 68 張，含 effect payload）→ `data/cards.buildings.json` 城市建築區塊
- [x] 建立擴充城市建築（10 種 29 張）+ 大教堂（1 張）→ 共 expansion 30 張
- [x] 建立職業牌資料（Prospector、Councillor、Trader、Builder、Producer）→ `data/cards.roles.json`
- [x] 建立規則常數（手牌上限 7、終局觸發 12 棟、初始手牌 4 張等）→ `data/rules.config.json`
- [x] 撰寫 JSON 欄位驗證測試 → `tests/unit/data/cards.test.ts`（id 唯一、cost/vp 合法範圍、必要欄位非空，15 cases）
- [x] `npm run lint` + `npm run build` + `npm run test` 全通過
- [ ] 補擴充新增生產建築（4 張）→ **待補**：來源文件標「種類依版本而定」，需確認確切貨物種類後加入

**Commit**：`chore(data): add buildings, roles JSON assets and field validation tests`

---

### Branch 3：`feat/api-foundation`
**目標**：實作全部 V0.1 可執行的 REST API（Auth + Room/Lobby + Game 狀態）

> **略過**（已決策，見 `docs/todo/done/pending-decisions.md`）：
> - email/password 系列（#1 決策：Discord-only，email/password 延後）
> - Token 長期保存（#3 決策：不保留長期 token，延後）
> - 歷史對局 / 統計 API（#2 決策：無後端 DB，延後）

#### 3-1 環境與 Auth

- [ ] 建立 `.env.example`（`DISCORD_CLIENT_ID`、`DISCORD_CLIENT_SECRET`、`DISCORD_REDIRECT_URI`、`SESSION_SECRET`）
- [ ] 建立 OAuth2 導向端點 `GET /api/auth/discord/login` → `server/api/auth/discord/login.get.ts`（302 redirect 到 Discord）
- [ ] 建立 OAuth2 Callback `GET /api/auth/discord/callback` → `server/api/auth/discord/callback.get.ts`（token exchange → httpOnly session cookie → redirect `/lobby`，失敗 → `/login?error=AUTH_ERROR`）
- [ ] 建立登出端點 `POST /api/auth/logout` → `server/api/auth/logout.post.ts`（清除 session cookie）
- [ ] 建立取得自身資料 `GET /api/users/info` → `server/api/users/info.get.ts`（從 session 讀取 discordId / username / avatar）
- [ ] 建立 `useAuth.ts` composable（`isLoggedIn`、`player`、`login()`、`logout()`）
- [ ] 補 OAuth callback mock 測試 → `tests/unit/server/discord-callback.nuxt.test.ts`

#### 3-2 房間 / 大廳 API

> 無後端 DB，房間資料以 Nitro server 端 in-memory Map 暫存（重啟後清空，V0.1 可接受）

- [ ] `GET /api/rooms` → `server/api/rooms/index.get.ts`（列出公開房間，可過濾 `status`）
- [ ] `POST /api/rooms` → `server/api/rooms/index.post.ts`（建立房間，需登入，回傳 `roomId` + `roomCode`）
- [ ] `GET /api/rooms/:roomId` → `server/api/rooms/[roomId].get.ts`（查詢房間詳細資料）
- [ ] `POST /api/rooms/:roomId/join` → `server/api/rooms/[roomId]/join.post.ts`（加入房間）
- [ ] `POST /api/rooms/:roomId/leave` → `server/api/rooms/[roomId]/leave.post.ts`（離開房間）
- [ ] `PUT /api/rooms/:roomId/ready` → `server/api/rooms/[roomId]/ready.put.ts`（切換準備狀態）
- [ ] `POST /api/rooms/:roomId/kick/:playerId` → `server/api/rooms/[roomId]/kick/[playerId].post.ts`（踢出玩家，房主限定）
- [ ] `POST /api/rooms/:roomId/start` → `server/api/rooms/[roomId]/start.post.ts`（開始遊戲，房主限定，需 4 人全準備）
- [ ] 建立 `useRoom.ts` composable（`rooms`、`currentRoom`、`createRoom()`、`joinRoom()`、`leaveRoom()`）

#### 3-2-b 房間 Socket.io 即時事件

> Namespace `/game`，與 REST API 共同構成完整的 `useRoom.ts`

- [ ] 初始化 Socket.io client，連線至 `/game` namespace → `app/plugins/socket.client.ts`
- [ ] 處理 `server:room_joined`（加入者收到房間完整狀態）
- [ ] 處理 `server:room_updated`（玩家加入 / 離開 / 準備狀態變動，廣播）
- [ ] 處理 `server:player_kicked`（被踢出通知，廣播）
- [ ] 處理 `server:room_closed`（房主離開，房間關閉，廣播）
- [ ] 實作聊天室：`client:send_message` 送出 / `server:chat_message` 接收（廣播）
- [ ] 將以上事件整合進 `useRoom.ts`（Socket 事件與 REST 狀態共用同一 composable）

#### 3-3 遊戲狀態 API

- [ ] `GET /api/games/:gameId/state` → `server/api/games/[gameId]/state.get.ts`（取得當前遊戲快照，斷線重連用）
- [ ] `GET /api/games/:gameId/result` → `server/api/games/[gameId]/result.get.ts`（取得終局結果與分數）
- [ ] `GET /api/games/:gameId/log` → `server/api/games/[gameId]/log.get.ts`（取得完整行動記錄）

#### 3-4 驗收

- [ ] `npm run lint` + `npm run build` + `npm run test` 全通過

**Commit**：`feat(api): implement auth, room/lobby, and game state REST endpoints`

---

## 分支策略

```
dev
├── chore/project-structure   ← 先完成，先 merge 回 dev
├── chore/game-data-json      ← 與 branch 1 並行（只動 data/ 和 tests/）
└── feat/api-foundation       ← 與 branch 1 並行，merge 前需等 branch 1 先進 dev
                                （依賴 types/ 中的 PlayerProfile、Room、GameSnapshot）
```

建議 merge 順序：branch 1 → branch 2 → branch 3
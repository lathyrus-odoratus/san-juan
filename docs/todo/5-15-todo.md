# 5/15 TODO

## 下週完成目標
~~1. 完成 `AGENTS.md` 設定與內容確認。~~
2. 將目前待決策項目全部定案，並同步更新到 SDD。
3. 在 `app/app.vue` 生成粗略畫面架構（先做靜態版面骨架）。

## 目前待決策項目
1. 自己手牌是否永遠固定在下方，且手機版是否改為單欄直向。
2. 其他玩家手牌是否只顯示張數（不顯示背面扇形）。
3. 點擊右下角 `i` icon 後，操作面板要用抽屜（drawer）還是彈窗（modal）。
4. 玩家資訊顯示模式預設值要用 `always` 還是 `hover`。
5. 建築說明模式預設值要用 `compact` 還是 `detailed`。

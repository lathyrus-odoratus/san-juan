# Todo 2026-07-03

> 建立日期:2026-06-26
> 預計執行日期:2026-07-03

## 已完成（06/26 收尾）
- [x] Branch 1 `chore/project-structure`：Nuxt 4 目錄骨架 + 核心型別（PR #2 merged）
- [x] Branch 2 `chore/game-data-json`：建築/職業/規則 JSON + 驗證測試（PR #3 merged，140 張：base 110 + expansion 30）

---

## 高優先（本週必須完成）

- [ ] **修正 cost-6 建築基本分錯誤**：`city_hall` / `guild_hall` / `palace` / `triumphal_arch`（基底）、`residence`（擴充）目前 `vp` 皆為 6，但這 5 棟 6 元建築**不應有 6 基本分**——分數只來自終局計分規則。
  - 修正 `data/cards.buildings.json`（這 5 張的 `vp`）
  - 同步檢查 `docs/rules/san-juan-victory.md` 步驟 1 表格「費用 6 的城市建築 | 6（加分另計）」是否一併修正
  - 調整 `tests/unit/data/cards.test.ts`（必要時加上「endgame_scoring 建築 vp 應為 0」的斷言）
- [ ] **Branch 3 `feat/api-foundation`**：實作 V0.1 REST API（Auth + Room/Lobby + Game 狀態）+ Socket.io 房間事件
  - 詳細子項見 `docs/todo/06-26-todo.md` Branch 3 區塊（3-1 環境與 Auth、3-2 房間/大廳 API、3-2-b Socket.io、3-3 遊戲狀態、3-4 驗收）

## 中優先（本週目標）

- [ ] **補擴充新增生產建築 4 張**：來源文件（`docs/rules/san-juan-cards.md`）標「具體種類依版本而定」，需先確認確切貨物種類，再：
  - 加入 `data/cards.buildings.json`
  - 更新 `data/rules.config.json` 的 `pendingExpansionProduction`（4 → 0）與 `expectedCardCount`（expansion 30→34、total 140→144）
  - 更新 `tests/unit/data/cards.test.ts` 預期張數

## 低優先（有餘力再做）

- [ ] 消除 GitHub Actions Node 20 deprecation 警告：升級 `actions/checkout`、`actions/setup-node` 至支援 Node 24 的版本（`.github/workflows/ci.yml`）

## 待釐清（需討論後才能動）

- [ ] cost-6 建築正確的基本分數值為何？（推測為 0，分數全來自終局計分）以官方 2nd edition 規則書為準
- [ ] 擴充新增生產建築 4 張的確切種類（哪些貨物 / 是否為新類型）

## 備註
- CI 已固定 npm 11 + Node 22（與本機一致），`npm ci` 不再因 lockfile 不同步而失敗
- `npm run test` / `test:e2e` 已加 `--passWithNoTests` / `--pass-with-no-tests`，待實際測試補上後仍相容

import type { GameEngine } from '~~/types/game'

/**
 * Deterministic rule engine. Must stay decoupled from UI and LocalStorage.
 * Skeleton — implemented in a later branch.
 */
export function useGameEngine(): GameEngine {
  throw new Error('useGameEngine: not implemented')
}
